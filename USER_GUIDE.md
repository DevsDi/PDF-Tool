# 用户使用指南

## 快速开始

### 运行

1. 解压 `PDF-Tool-Portable.7z` 到任意目录
2. 双击 `PDF全能箱.exe` 运行

> ⚠️ PDF转Word、转Excel、加水印功能需要安装 Python 3 + 依赖库：
> `pip install PyMuPDF pdf2docx pdfplumber pandas openpyxl`

### 主界面

```
┌─────────────────────────────────────────────────┐
│  PDF全能箱                                        │
├──────────┬───────────────────────────────────────┤
│ PDF合并   │                                        │
│ PDF拆分   │     【当前功能页面内容】                │
│ 页面排序   │                                        │
│ 格式转换   │                                        │
│ PDF压缩   │                                        │
│ 添加水印   │                                        │
│          │                                        │
│ ◀ 收起   │                                        │
├──────────┴───────────────────────────────────────┤
```

左侧导航栏 6 个功能模块，可收起。支持拖拽文件到页面直接上传。

---

## 功能详解

### 1. PDF合并

将多个PDF合并为一个文件。

1. 点击「PDF合并」→ 拖拽或点击上传多个PDF
2. 拖拽调整合并顺序
3. 选择保存位置
4. 点击「开始合并」

### 2. PDF拆分

将一个PDF拆分为多个文件，支持三种方式：

| 方式 | 示例 | 结果 |
|------|------|------|
| 按页数 | 每5页 | 20页PDF → 4个文件 |
| 按范围 | 1-5, 6-10 | 生成2个文件 |
| 提取页面 | 1, 3, 5-7 | 提取5页为一个文件 |

### 3. 页面排序

调整PDF页面顺序或删除不需要的页面。

1. 上传PDF后自动显示所有页面缩略图
2. 使用上移/下移按钮调整顺序
3. 点击删除按钮移除不需要的页面
4. 选择保存位置 → 点击「确认排序」

### 4. 格式转换

**PDF转Word**：保留格式、表格、图片。适合需要编辑的文档。

**PDF转Excel**：提取PDF中的表格数据。适合数据统计。

> 需要安装 Python 环境

### 5. PDF压缩

减小PDF文件体积。

| 压缩级别 | 质量 | 压缩率 | 适用场景 |
|----------|------|--------|----------|
| 低压缩 | 高 | ~20% | 需保持高清 |
| 中压缩 | 中 | ~50% | 一般使用 |
| 高压缩 | 低 | ~70% | 文件传输 |

### 6. 添加水印

为PDF添加自定义文字水印。

**可配置项：**

| 选项 | 说明 | 默认值 |
|------|------|--------|
| 水印文字 | 自定义内容 | 内部资料 |
| 位置 | 居中/斜向平铺/平铺/顶部/底部 | 斜向平铺 |
| 字体大小 | 20-100px | 40 |
| 透明度 | 10%-90% | 30% |
| 旋转角度 | -90°~90°（斜向平铺时有效） | 45° |
| 颜色 | 自定义颜色 | 灰色 #808080 |

> 需要安装 Python + PyMuPDF

---

## 常见问题

### Q: 打开应用后白屏？

确认已完整解压所有文件，检查杀毒软件是否拦截。

### Q: 格式转换/加水印报错？

需要安装 Python 3 及依赖：
```bash
pip install PyMuPDF pdf2docx pdfplumber pandas openpyxl
```

### Q: 加水印后看不到水印？

- 检查透明度设置是否过低（建议30%以上）
- 检查水印颜色是否与页面背景色接近
- 尝试增大字体大小

### Q: 拖拽文件不生效？

部分安全软件可能阻止拖拽，可点击上传区域使用文件选择对话框。

---

## 功能依赖一览

| 功能 | 需要Python | 需要网络 |
|------|-----------|---------|
| PDF合并 | ❌ | ❌ |
| PDF拆分 | ❌ | ❌ |
| 页面排序 | ❌ | ❌ |
| PDF压缩 | ❌ | ❌ |
| PDF转Word | ✅ | ❌ |
| PDF转Excel | ✅ | ❌ |
| 添加水印 | ✅ | ❌ |
